SELECT SUM(cantidad)
FROM productosdefactura
WHERE nom_producto = 'DETERGENTE'
--Unidades vendidas: 21

SELECT SUM(cantidad)
FROM productosdefactura
WHERE nom_producto = 'JABON LOZA'
--Unidades vendidas: 30

SELECT SUM(cantidad)
FROM productosdefactura
WHERE nom_producto = 'JABON MANOS'
--Unidades vendidas: 12
